
import scala.util.Random
import q20.removeAt
object q23{
  def extractRandom[Int](n:Int, ls:List[Int]): List[Any] = {

  }

  def get[A](n: Int, ls: List[A]): Any = {

  }
  def main(args:Array[String]): Unit ={
    val ls  = List(1,2,3,4,5,6,7);
    print("3 randomly selected numbers are : " + extractRandom(3,ls))
  }
}